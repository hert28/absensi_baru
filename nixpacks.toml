# Nixpacks configuration untuk Railway
# Menambahkan system libraries yang dibutuhkan dlib (face_recognition)

[phases.setup]
nixPkgs = ['cmake', 'gcc', 'xorg.libX11', 'xorg.libXext', 'xorg.libXrender', 'xorg.libSM', 'xorg.libICE']
